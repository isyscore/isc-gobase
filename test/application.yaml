api-module: test
server:
  port: 31000
  gin:
    mode: release
  logger:
    level: debug

base:
  application:
    name: "test"
  api:
    prefix: /api/core
  endpoint:
    # 健康检查处理，默认关闭
    health:
      enable: true
    # 配置的动态实时变更，默认关闭
    config:
      enable: true

