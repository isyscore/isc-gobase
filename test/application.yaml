api-module: api/test
server:
  port: 31000
base:
  application:
    name: "test"
  endpoint:
    # 健康检查处理，默认关闭
    health:
      enable: false
    # 配置的动态实时变更，默认关闭
    config:
      enable: true





